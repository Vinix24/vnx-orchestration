governance_profile: light
isolation:
  scope_type: business_folder
  allowed_paths:
    - "agents/linkedin-writer/"
    - ".vnx-data/unified_reports/"
  denied_paths:
    - "scripts/"
    - "dashboard/"
    - ".claude/"
