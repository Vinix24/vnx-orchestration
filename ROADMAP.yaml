# ============================================================================
# ROADMAP.yaml is the SINGLE SOURCE OF TRUTH for the VNX feature roadmap.
#
# It is the only hand-edited planning surface. Two views are GENERATED from it
# and must never be hand-edited:
#   - FEATURE_PLAN.md  <- scripts/build_feature_plan.py
#   - PR_QUEUE.md      <- scripts/build_pr_queue.py
#
# After editing this file, regenerate both views and commit them together:
#   python3 scripts/build_feature_plan.py
#   python3 scripts/build_pr_queue.py
# CI guards this: tests/test_roadmap_consistency.py runs the generators with
# --check and fails the build if either view drifts from this file.
#
# claudedocs/*.md (e.g. MASTER-PRELAUNCH-PLAN.md, DISPATCH-UNIFICATION-*.md) are
# detailed working/design docs, NOT status SSOT. Their operational status is
# folded into launch_state + features[] here. When a working doc is executed,
# archive it under claudedocs/_archive/ and let this file carry the live state.
#
# This anti-drift rule exists because three planning baselines (this file,
# the generated views, and the claudedocs prelaunch plan) had drifted apart:
# launch_state falsely read "production-validated" while ~10 launch PRs were
# still open. Keep status in ONE place (here); generate the rest.
#
# ADR-007: every new central-DB table needs a composite UNIQUE/PK over
# project_id. Cite it explicitly in review-gate prompts for schema work.
# ============================================================================

roadmap_id: vnx-roadmap-autopilot
title: VNX Multi-Feature Roadmap

launch_state:
  version: "1.0.0"
  date: "2026-06-27"             # last update to this launch_state block
  status: "pre-launch"            # gate-clear; awaiting operator PyPI publish-go (LB-3)
  target_date: "2026-06-09"       # original target; publish-ready since 2026-06-10, still operator-gated
  last_verified: "2026-06-27"
  verified_against: "origin/main@9e5a9302"
  note: >
    Still gate-clear; the only thing between 1.0.0 and release is the
    operator-only PyPI publish (LB-3). Since 2026-06-10 the codebase kept
    moving while staying gate-clear: a 4-day hardening sprint (#913-#920), an
    HN-readiness docs sweep + cleanup (#921/#922), a migration refactor (#924),
    the full intelligence build-program steps 0-5 (#923 render-gap, #925
    rank-then-budget, #926 doc_relevant, #927/#928 tagging, #929 outcome-
    grounding, #930/#931 scout pre-pass), and a codex-triaged test-suite-green
    sweep (#932-#934) establishing the no-bare-FAIL standard. Every PR was
    kimi-diff-gated + CI-green. The 2026-06-09/10 pre-publish sweep history
    (LB-3..LB-7, #833-#840) is in claudedocs/2026-06-09-RUNDOWN-1.0-PREPYPI-
    SWEEP.md and this block's git history.
  launch_blockers:
    - "LB-3 (operator): PyPI publish go (artifacts verified + staged) -> pip install from PyPI check -> git tag v1.0.0 + GitHub release. Optional: central-install refresh + TestPyPI dry-run first. LB-4..LB-7 resolved 2026-06-10 (#833/#835/#836/#839/#840, see launch-sweep-blockers)."

features:
  # ---- Shipped foundation (pre-1.0) ----
  - feature_id: roadmap-autopilot
    title: Roadmap Autopilot, Auto-Next Feature Loading, and Multi-Reviewer Gates
    plan_path: roadmap/features/roadmap-autopilot/FEATURE_PLAN.md
    branch_name: feature/roadmap-autopilot-review-gates
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
      - claude_github_optional
    depends_on: []
    milestone: "1.0"
    status: shipped-dark
    notes: >
      RA-1..6 merged (project_id stamp, branch+worktree provisioning, gate enforcement,
      human-gate primitive, step driver, autopilot tick). RA-6 autopilot_tick ships dark
      (default off, env-gated). RA-3b closed 4 advance-gate bypass holes. User-facing
      exposure deferred to 1.2 (needs smart-routing default-on).

  - feature_id: fut-1
    title: "Track-layer schema + DAL + CLI + audit-ordering (PR-FUT-1)"
    branch_name: feat/fut-1-track-schema
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on: []
    milestone: "1.0"
    status: done

  - feature_id: fut-2
    title: "Track tenant-scoping per ADR-007 — composite PK over project_id, seeding from roadmap, composite-FK dispatches.track → tracks(track_id, project_id), project-aware CLI"
    branch_name: feat/fut-2-track-tenant-scope
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - fut-1
    milestone: "1.0"
    status: done
    notes: "FUT-2a (composite PK + ADR-007 compliance) and FUT-2b (structural regression tests) both merged."

  # ---- 1.0 clean-launch queue (in-flight; mapped to open PRs) ----
  - feature_id: launch-wave0-hygiene
    title: "1.0 launch — Wave 0 hygiene: untrack .venv + scratch, strip emoji/buzzwords (keep FEATURE_PLAN/PR_QUEUE tracked)"
    branch_name: dispatch/20260530-180455-wave0
    risk_class: low
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on: []
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#756"
        title: "chore(wave0): untrack .venv + scratch, strip emoji/buzzwords"
        status: merged
        risk_class: low
    notes: "Merge first; unblocks a clean security-grep baseline. #755 lesson: keep FEATURE_PLAN.md + PR_QUEUE.md tracked, clean their content only."

  - feature_id: launch-renames
    title: "1.0 launch — Wave 1 renames: _vN files -> canonical names + compat shims (remove in 1.0.1)"
    branch_name: dispatch/20260530-202158-renames
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-wave0-hygiene
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#759"
        title: "refactor(rename): _vN files -> canonical names + compat shims"
        status: merged
        risk_class: high
    notes: >
      The rebase bomb (60-73 files incl live supervisors + bin/vnx). Merged alone;
      grep-zero across all filetypes + 4 runtime smoke-tests + net-deletion
      sanity check verified before subsequent PRs rebased.

  - feature_id: launch-scrub
    title: "1.0 launch — Wave 1 scrub: remove residual private-project artifacts from repo + wheel"
    branch_name: dispatch/20260530-201207-scrub-t
    risk_class: low
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - launch-renames
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#758"
        title: "chore(scrub): remove residual private-project artifacts"
        status: merged
        risk_class: low

  - feature_id: launch-readme
    title: "1.0 launch — Wave 1 credibility: README control-plane repositioning + ADR section + architecture diagram"
    branch_name: dispatch/20260530-183158-readme
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - launch-renames
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#757"
        title: "docs(readme): control-plane repositioning + ADR section + arch diagram"
        status: merged
        risk_class: medium
    notes: "Re-scope the headline to what is true: claude-subprocess is the reference lane; do not oversell the tmux-spawn lane as a proven default."

  - feature_id: launch-governance-core
    title: "1.0 launch — Wave 1b: role-based manager block + auto-inject T0-action footer (enforcement, not convention)"
    branch_name: dispatch/20260530-204128-wave1b
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-renames
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#760"
        title: "feat(wave1b): role-based manager block + auto-inject T0-action footer"
        status: merged
        risk_class: high
    notes: "Freeze trio with #759/#761. Drops Track:/Terminal: for Role:. Ships scripts/lib/dispatch_footer.py (avoid a colliding second footer in parity work)."

  - feature_id: launch-provider-intel
    title: "1.0 launch — Wave 2: provider-aware intelligence + receipt token/cost + report linkage (ADR-007 provider column)"
    branch_name: dispatch/20260530-205917-wave2core
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-renames
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#761"
        title: "feat(wave2): provider-aware intelligence + receipt token/cost"
        status: merged
        risk_class: high
    notes: "Freeze trio. Adds provider column (ADR-007 composite-key compliant). Does NOT inject the non-Claude skill body; that gap closes in Option B parity (prepare())."

  - feature_id: launch-kimi-robust
    title: "1.0 launch — Wave 2: kimi 1.44.0 robust spawn + fail-loud on empty extraction (drop --yolo)"
    branch_name: fix/kimi-robust-spawn
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-renames
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#763"
        title: "fix(kimi): robust spawn + fail-loud on empty extraction"
        status: merged
        risk_class: medium
    notes: "First instance of the provider-hardening program. Deeper content-block info-bug fix + MAJOR-2 regression test deferred to 1.0.1 (see kimi-deepfix-major2)."

  - feature_id: launch-capability-interim
    title: "1.0 launch — Wave 2: scope worker capabilities (drop --dangerously-skip-permissions, empty ambient MCP)"
    branch_name: fix/worker-capability-scoping
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-renames
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#764"
        title: "fix(worker): scope capabilities — drop --dangerously-skip-permissions, empty MCP"
        status: merged
        risk_class: high
    notes: >
      #762 closed as duplicate (same three hot files, conflicting). Removes the
      MCP blast radius and the most quotable counter-example to the glass-box
      claim; does NOT enforce per-profile file-write scope (full binding is 1.1,
      see capability-binding-full).

  - feature_id: launch-deepseek-lane
    title: "1.0 launch — governed DeepSeek-harness lane (own-key, key-auth, MCP-off, account-safe)"
    branch_name: feat/deepseek-harness-lane
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-governance-core
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#765"
        title: "feat(providers): governed DeepSeek-harness lane"
        status: merged
        risk_class: medium
    notes: "Native Anthropic-compat harness lane; never on the OAuth subscription. Measured 0 calls to api.anthropic.com under key-auth + MCP-off."

  - feature_id: launch-adr020-contract
    title: "1.0 launch — ADR-020 parallel multi-track execution contract (design-ratified; implementation 1.2)"
    branch_name: dispatch/20260530-105008-track-doc
    risk_class: low
    merge_policy: human
    review_stack:
      - gemini_review
    depends_on: []
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#751"
        title: "docs(adr): ADR-020 parallel multi-track execution contract"
        status: merged
        risk_class: low
    notes: "Doc-only. Must read 'design-ratified, implementation 1.2' in-doc so it is not mistaken for shipped behavior. Implementation lives in parallel-track-scheduler (1.2)."

  - feature_id: launch-tmux-spawn-enrichment
    title: "1.0 launch — fix(tmux-spawn): reuse skill+intelligence enrichment + emit unified_report (dogfood default lane)"
    branch_name: fix/tmux-spawn-enrichment
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - launch-governance-core
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#766"
        title: "fix(tmux-spawn): reuse skill+intelligence enrichment + emit unified_report"
        status: merged
        risk_class: medium
    notes: "Dogfood fix for the default tmux-spawn lane. Reuses existing enrichment path instead of duplicating."

  - feature_id: launch-tmux-default-routing
    title: "1.0 launch — feat(dispatch): route default Claude lane through enriched tmux-spawn (dogfood + Wave-1b)"
    branch_name: feat/tmux-default-routing
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - launch-tmux-spawn-enrichment
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#767"
        title: "feat(dispatch): route default Claude lane through enriched tmux-spawn"
        status: merged
        risk_class: medium
    notes: "Default Claude lane now routes through the enriched tmux-spawn path. Builds on #766 enrichment."

  - feature_id: launch-readme-honesty
    title: "1.0 launch — docs(readme): honesty pass on tmux-lane maturity, lane uniformity, OpenRouter scope"
    branch_name: docs/readme-honesty
    risk_class: low
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - launch-tmux-default-routing
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#769"
        title: "docs(readme): honesty pass on tmux-lane maturity, lane uniformity, OpenRouter scope"
        status: merged
        risk_class: low
    notes: >
      Removes oversell of tmux-spawn as a proven default. Accurately describes
      lane maturity tiers and OpenRouter scope. Required before public launch.
      Fix #770 (dispatch prompt-before-scope-flags lane repair) also merged to
      main as a 1.0 fix; no separate feature entry (scope: targeted dispatch bug fix).

  - feature_id: tmux-lane-structural-refactor
    title: "1.0 launch — June-15-critical: tmux subscription-lane PREPARE+GOVERN structural completion (tmux lane only, strangler-fig)"
    branch_name: feat/tmux-lane-structural-refactor
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-governance-core
      - launch-provider-intel
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#771"
        title: "feat(tmux): PREPARE structural completion (tmux lane)"
        status: merged
        risk_class: high
      - pr_id: "#772"
        title: "feat(tmux): GOVERN structural completion (tmux lane)"
        status: merged
        risk_class: high
      - pr_id: "#773"
        title: "feat(tmux): RECEIPT structural completion (tmux lane)"
        status: merged
        risk_class: high
      - pr_id: "#774"
        title: "feat(tmux): delivery structural completion (tmux lane)"
        status: merged
        risk_class: high
      - pr_id: "#775"
        title: "feat(tmux): CAPTURE structural completion (tmux lane)"
        status: merged
        risk_class: high
      - pr_id: "#776"
        title: "feat(tmux): uniform-receipts structural completion (tmux lane)"
        status: merged
        risk_class: high
    notes: >
      June-15-critical. The Anthropic OAuth change (June 15, 2026) makes claude-tmux-spawn
      the only subscription-preserving lane and the default (#767). Builds on #766
      (skill+intelligence enrichment) and #767 (default-routing). Scope: full PREPARE on
      the tmux lane — permission preamble + worker-rules footer + report-contract directive;
      full GOVERN close-out — guaranteed unified_report + validate_body before receipt
      emission; env/receipt-consistency fix — VNX_STATE_DIR inherited into the tmux-spawn
      worker so the receipt lands where the orchestrator reads it. Strangler-fig:
      flag-gated + shadow-mode + burn-in before hard-enforce. Scoped to the tmux lane
      ONLY. Full LaneRouter / all-lanes PREPARE/GOVERN unification and legacy deletion
      defer to opt-a-envelope-extraction (1.1) and launch-option-b-parity (1.1). Provider
      lanes (codex/gemini/kimi/litellm) are NOT June-15-critical. Shipped via #771 PREPARE,
      #772 GOVERN, #773 RECEIPT, #774 delivery, #775 CAPTURE, #776 uniform-receipts
      (all merged 2026-06-01).

  - feature_id: launch-option-b-parity
    title: "Option B parity (all lanes): uniform prepare()/govern() across subprocess + tmux + all provider lanes"
    branch_name: feat/option-b-dispatch-parity
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - tmux-lane-structural-refactor
      - launch-governance-core
      - launch-provider-intel
    milestone: "1.1"
    status: planned
    notes: "Extends PREPARE/GOVERN uniformity to subprocess + provider lanes (post-1.0)."

  # ---- Post-1.0 roadmap (planned; ordered with dependencies) ----
  - feature_id: kimi-deepfix-major2
    title: "Kimi #763 info-bug deep fix + MAJOR-2 regression test"
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-kimi-robust
      - launch-provider-intel
    milestone: "1.0.1"
    status: planned
    notes: "Content-block edge cases not covered by the 1.44.0 stream-json parser fix; dedicated regression suite."

  - feature_id: log-rotation
    title: "Log-rotation fix: event-stream ring-buffer growth + receipt log size bounds"
    risk_class: low
    merge_policy: human
    review_stack:
      - gemini_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    pr_queue:
      - pr_id: "#779"
        title: "feat(ops): opt-in DB maintenance for quality_intelligence.db (GAP 3a)"
        status: merged
        risk_class: low
    notes: >
      Operational hygiene; not a fresh-install blocker. GAP 3a (opt-in DB maintenance,
      scripts/vnx_db_maintenance.py) shipped as #779 (merged 2026-06-01). GAP 3b (ledger
      rotation + per-append hash-chain) deferred — see receipt-hashchain-wire.

  - feature_id: opt-a-envelope-extraction
    title: "Option A envelope extraction: dispatch_envelope.py + LaneRouter + delete legacy duplicated PREPARE/GOVERN"
    plan_path: claudedocs/DISPATCH-UNIFICATION-opus.md
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-option-b-parity
    milestone: "1.1"
    status: in-progress
    pr_queue:
      - pr_id: "#789"
        title: "feat(dispatch): PR-1 flag-gated dispatch envelope (codex lane, VNX_UNIFIED_ENVELOPE, legacy default)"
        status: merged
        risk_class: high
    notes: >
      Code-organization payoff (no duplication), not new behavior. Strangler-fig
      behind VNX_UNIFIED_ENVELOPE=1, legacy default until burn-in; delete legacy
      blocks only after. Flag-gated default-off. PR-1 codex-lane merged (#789,
      2026-06-02). PR-2 claude-subprocess lane open (role-forward blocker). PR-3
      tmux adapter + PR-9 /pending routing remain. ~20k total (dispatch
      unification). Previously: Option A envelope extraction; the merge of #789 is
      the first codex-lane PR for the unified envelope.

  - feature_id: capability-binding-full
    title: "Full role->capability binding: mcp_servers allowlist + permission_mode per role + un-dead generate_claude_settings + per-adapter materialization + permission_enforcement receipt field"
    plan_path: claudedocs/WORKER-CAPABILITY-SCOPING-DESIGN.md
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - opt-a-envelope-extraction
    milestone: "1.1"
    status: planned
    notes: "Completes least-privilege beyond the 1.0 capability interim; needs Option A's PREPARE seam. Enforces per-profile file-write scope the interim does not."

  - feature_id: per-folder-agents
    title: "Per-folder agents: folder-addressable role = skill + permission-profile + model"
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - capability-binding-full
    milestone: "1.1"
    status: planned
    notes: "Depends on the role->capability binding existing. Build the 1.0 role layer folder-addressable so this slots on without rebuild. Maps to CC-native .claude/agents/*.md tools: frontmatter."

  - feature_id: openrouter-arbitrary
    title: "OpenRouter-arbitrary / OpenAI-compat proxy-gated lane class"
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - opt-a-envelope-extraction
    milestone: "1.1"
    status: planned
    notes: "Distinct lane class (Anthropic<->OpenAI proxy, ~1.1x overhead) from the native Anthropic-compat harnesses shipped in 1.0. Inherits uniform prepare/govern."

  - feature_id: provider-hardening
    title: "Provider hardening: fail-loud on empty extraction + CI-canary per CLI version + version-pin"
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - launch-kimi-robust
    milestone: "1.1"
    status: planned
    notes: "Systematizes the kimi #763 point fix: every provider fails loud on empty extraction; a CI canary dispatches a trivial task through each lane per CLI version; CLI versions pinned in provider_constraints.yaml."

  - feature_id: bench-v2-smart-lanes
    title: "Smart Lanes field-tests benchmark"
    risk_class: low
    merge_policy: human
    review_stack: []
    depends_on:
      - PR-SR-CAPTURE
      - OI-155
    milestone: "1.1"
    status: planned
    notes: "Realistic-bench suite at scripts/benchmark/field-tests/ for lane calibration."

  - feature_id: gemma-4-12b-integration
    title: "Gemma 4 12B local-lane evaluation"
    risk_class: low
    merge_policy: human
    review_stack: []
    depends_on:
      - bench-v2-smart-lanes
    milestone: "1.1"
    status: planned
    notes: "Evaluate Gemma 4 12B as local worker / router / enrichment-engine, gated on bench-v2."

  - feature_id: aef-style-enrichment-layer
    title: "Dispatch enrichment between staging and pending"
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - gemma-4-12b-integration
    milestone: "1.2"
    status: planned
    notes: "Discovery/enrichment loop before dispatch promotion."

  - feature_id: usage-aware-routing
    title: "Usage/budget aggregator + usage-aware routing"
    risk_class: medium
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - provider-hardening
    milestone: "1.2"
    status: planned
    notes: "Centralize quota state, feed smart-router to skip exhausted lanes."

  # ---- 2026-06-01 backfill (post-1.0 work) ----
  - feature_id: gap4-self-learning-reactivation
    title: "GAP 4: reactivate self-learning loop — nightly intelligence pipeline cron"
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#780"
        title: "feat(intelligence): install nightly intelligence pipeline cron (GAP 4 — reactivate self-learning loop)"
        status: merged
        risk_class: medium
    notes: >
      Installed nightly intelligence pipeline cron (4am, Mac mini), deterministic/0-LLM,
      human-gated via pending_edits.json. 4 auxiliary pipeline phases regressed during
      dormancy -> OI-2331.

  - feature_id: gap6-submit-reliability
    title: "GAP 6: bounded guarded-retry + adaptive settle for tmux submit reliability"
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#781"
        title: "fix(tmux): bounded guarded-retry + adaptive settle for submit reliability (GAP 6)"
        status: merged
        risk_class: medium
    notes: "Bounded guarded-retry + adaptive paste-settle in tmux_interactive_dispatch.py; live burn-in 5/5 submits."

  - feature_id: gap5-report-pipeline-decision
    title: "GAP 5: report_pipeline retention decision (keep — load-bearing)"
    risk_class: low
    merge_policy: human
    review_stack: []
    depends_on: []
    milestone: "1.0.1"
    status: done
    notes: >
      Decision: KEEP report_pipeline (load-bearing: active Stop hook +
      subprocess_adapter.trigger_report_pipeline + gate_artifacts sidecar OI-1066).
      Future consolidation -> opt-a-envelope-extraction (1.1). No code change.

  - feature_id: tracks-layer-activation
    title: "Structural-doctor: repair v26-but-absent-tracks divergence + activate dark FUT-1/2 tracks layer"
    risk_class: high
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - fut-1
      - fut-2
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#782"
        title: "feat(doctor): structural-doctor for tracks-layer activation (dry-run default, backup-on-apply)"
        status: merged
        risk_class: high
    notes: >
      Structural-doctor (scripts/vnx_structural_doctor.py) repaired the
      v26-but-absent-tracks divergence and activated the dark FUT-1/2 tracks layer
      on the live coordination DB (4 track tables + dispatches.output_ref/output_kind;
      291 dispatches preserved; backup + ADR-005 audit). Plan:
      claudedocs/PLANNING-OBJECT-MODEL-SYNTHESIS-2026-06-01.md.

  - feature_id: planning-future-state-layer
    title: "Planning object model: TRACK(=feature) -> DISPATCH(execution leaf) -> output+receipt future-state layer"
    plan_path: claudedocs/PLANNING-OBJECT-MODEL-SYNTHESIS-2026-06-01.md
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - tracks-layer-activation
    milestone: "1.x"
    status: shipped-dark
    pr_queue:
      - pr_id: "#787"
        title: "feat(planning): Phase 1 — tracks seeder + horizon + deliverables view + vnx objective list"
        status: merged
        risk_class: high
      - pr_id: "#790"
        title: "feat(planning): Phase 2 — deliverable plane + proposed->ready human gate (vnx deliverable add/list/promote)"
        status: merged
        risk_class: high
      - pr_id: "#791"
        title: "feat(dashboard): planning kanban — objectives/deliverables/open-items by horizon (reuses design system)"
        status: merged
        risk_class: high
      - pr_id: "#793"
        title: "feat(planning): Phase 3 — advisory rollup reconciler (derived_status, idempotent, never auto-writes ROADMAP)"
        status: merged
        risk_class: high
    notes: >
      Build-complete pending live activation + envelope unification. Status
      corrected to shipped-dark on 2026-06-10 (PM-reconcile): all 4 PRs
      (#787/#790/#791/#793) merged, reconciler reported declared 'planned' vs
      derived 'done' drift for 7 days. Live activation (dispatch<->track
      linkage, vnx track done, oi-close) tracked in oi-lifecycle-closure.

  - feature_id: receipt-hashchain-wire
    title: "GAP 3b: per-append hash-chain wire + epoch rotation + verify_history + fork-tests"
    plan_path: claudedocs/GAP-3b-wire-spec-1.0.1.md
    risk_class: high
    merge_policy: human
    review_stack:
      - gemini_review
      - codex_gate
    depends_on:
      - log-rotation
    milestone: "1.0.1"
    status: in-progress
    pr_queue:
      - pr_id: "#786"
        title: "feat(receipts): per-append hash-chain wiring, flag-gated VNX_CHAIN_RECEIPTS (GAP 3b)"
        status: merged
        risk_class: high
    notes: >
      GAP 3b. Per-append hash-chain wired into the receipt append (lock-scoped in
      idempotency.py), flag-gated VNX_CHAIN_RECEIPTS default-off (#786 merged
      2026-06-02). Epoch-rotation of the live ledger + verify_history + concurrent
      fork-tests still pending (1.0.1 remainder). 4-architect-decided
      DROP-claim-now-wire-1.0.1. README hash-chain claim softened in DOC-PASS.

  - feature_id: provider-lanes-doc-pass
    title: "Provider-lanes USP doc + README honesty fix + ROADMAP/docs reflect tracks-activation"
    risk_class: low
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on: []
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#785"
        title: "docs: provider-lanes USP doc + README hash-chain honesty fix"
        status: merged
        risk_class: low
    notes: >
      Operator-wanted BEFORE publish. NEW provider-lanes USP doc
      (claude-subprocess/claude-tmux-subscription/codex/gemini/kimi/deepseek-harness +
      ollama-resolver; no SDK; June-15 driver; when-which-lane incl. analysis-vs-commit
      lane behavior) + README honesty fix (hash-chain claim -> verification-tooling) +
      ROADMAP/docs reflect tracks-activation. Merged #785 on 2026-06-01.

  # ---- 2026-06-02 overnight merges (post-backfill work) ----
  - feature_id: report-to-receipt-converter
    title: "Universal report->receipt converter: validates report_body_contract, no hooks, generic conversion"
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#788"
        title: "feat(governance): unified_report as universal interface — AGENTS.md report-contract + generic report->receipt conversion (no hooks)"
        status: merged
        risk_class: medium
    notes: >
      Universal report->receipt converter. Validates the report_body_contract
      (scripts/lib/report_body_contract.py) and converts any compliant unified_report
      to a receipt without hooks or side effects. Merged #788 on 2026-06-02.

  - feature_id: oi-2331-intelligence-repair
    title: "OI-2331: repair 4 regressed nightly intelligence pipeline phases after GAP 4 reactivation dormancy"
    risk_class: medium
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - gap4-self-learning-reactivation
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#792"
        title: "fix(intelligence): repair 4 regressed nightly pipeline phases (OI-2331)"
        status: merged
        risk_class: medium
    notes: >
      4 auxiliary intelligence pipeline phases regressed during the self-learning
      loop dormancy period (GAP 4). Repaired and verified. Merged #792 on 2026-06-02.

  - feature_id: oi-2328-atomic-prune
    title: "OI-2328: atomic prune transaction for DB maintenance (vnx_db_maintenance.py)"
    risk_class: low
    merge_policy: human
    review_stack:
      - codex_gate
    depends_on:
      - log-rotation
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#794"
        title: "fix(db-maintenance): atomic prune transaction (OI-2328)"
        status: merged
        risk_class: low
    notes: >
      Atomic prune transaction for the DB maintenance script
      (scripts/vnx_db_maintenance.py). Ensures transactional safety on prune
      operations. Merged #794 on 2026-06-02.

  - feature_id: sales-copilot-vnx-upgrade
    title: "Sales-copilot project — VNX install upgrade to post-1.0"
    risk_class: low
    merge_policy: human
    review_stack: []
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: "Bring sales-copilot's VNX install current with main (~3 weken gap, MC-cutover shape)."

  # ---- 2026-06-10 PM-reconcile: pre-publish sweep + ops-sweep + root-causes ----
  # Source docs: claudedocs/2026-06-09-RUNDOWN-1.0-PREPYPI-SWEEP.md and
  # claudedocs/2026-06-09-OPS-SWEEP-DASHBOARD-OBS-ROADMAP-OI.md.

  - feature_id: launch-sweep-blockers
    title: "1.0 pre-publish blocker fix-set: wheel client-data scrub, audit_chain unchained-status, init hooks-wiring, CHANGELOG 1.0.0"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: done
    pr_queue:
      - pr_id: "#836"
        title: "fix(scrub): remove client project data from wheel + docs index path (LB-4)"
        status: merged
        risk_class: medium
      - pr_id: "#840"
        title: "fix(audit-chain): distinguish unchained from broken ledger in verify (LB-5)"
        status: merged
        risk_class: medium
      - pr_id: "#835"
        title: "fix(init): wire governance hooks block into scaffolded settings.json (LB-6)"
        status: merged
        risk_class: medium
      - pr_id: "#839"
        title: "docs(changelog): add [1.0.0] release entry (LB-7)"
        status: merged
        risk_class: low
    notes: >
      CLOSED 2026-06-10: all four LB-fixes merged the same night, each
      kimi-gated (governed lane, receipts 20260610-gate-kimi-pr835/836/837/
      838/840 in t0_receipts.ndjson; two gate findings fixed forward, one
      false-positive FAIL overruled with empirical proof on #838). LB-3
      re-run GREEN on main@bfe37501 proved all four on the artifact:
      client-data grep zero-hit, audit_chain 'unchained' exit 0, hooks wired
      in fresh scaffold, [1.0.0] changelog present. Panel attribution: A2/A3
      unanimous ship-blockers (Opus + Kimi-op + Kimi-sec); A1/A4 T0-verified.

  - feature_id: oi-222-ssrf-hardening
    title: "OI-222: SSRF hardening — is_global instead of is_private (CGNAT 100.64/10) + DNS-rebinding IP-pinning, then wire url_policy into fetch paths"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      url_policy.py ships unwired (honestly documented, no live exposure).
      F1: ip.is_private misses CGNAT -> use not ip.is_global
      (url_policy.py:180). F2: validate-then-fetch window -> pin resolved IP
      at connection level when wiring. Sweep R8 re-confirmed both gaps.

  - feature_id: oi-223-lane-safety-loader
    title: "OI-223: lane_safety loader — make routing_policy.yaml lane_safety block live (force_headless now hardcoded in lane_adapter.HEADLESS_FORCED_MODELS)"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      lane_safety + fallback_chain blocks in routing_policy.yaml are advisory;
      no dispatcher reads them. Also add the missing STATUS comment on
      fallback_chain (sweep R5) and fix the review-analysis lane conflict with
      kimi-via-cli-only (covered in sweep-hardening-batch1, keep in sync).

  - feature_id: oi-224-bench-runner-edges
    title: "OI-224: bench-runner edge-cases — retry isolation (headless shared checkout), stale-report prefix match, worktree-removal result"
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: "Carried from 2026-06-09 handoff; formal registration was pending until this PM-reconcile."

  - feature_id: oi-225-bench-harness-generalize
    title: "OI-225: generalize bench harness into a shippable feature (seeds-free, token-free, bring-your-own-tasks)"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.1"
    status: planned
    notes: "Benchmark stays repo-only in 1.0 (#832 excluded it from the wheel). This is the 1.1 path back to a package feature."

  - feature_id: nightly-pipeline-python-compat
    title: "Nightly pipeline phase 0-schema-init: Python 3.9 crash fix (Path | None at quality_db_init.py:905) + crontab PATH + version guard"
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#838"
        title: "fix(nightly): python3.9 compat (from __future__ import annotations) + version guard"
        status: merged
        risk_class: low
      - pr_id: "#842"
        title: "fix(schema-init): view-ordering on legacy DBs unblocks v22/v23 (phase-0 failure mode 2)"
        status: merged
        risk_class: low
    notes: >
      CLOSED 2026-06-11: BOTH phase-0 failure modes resolved. Mode-2 (Python
      3.9 crash): #838 merged (from __future__ import annotations +
      python>=3.10 warn-guard; kimi-gate FAIL was a false positive, overruled
      with empirical guard-proof in the PR comments) and the crontab PATH line
      (/opt/homebrew/bin first) applied by T0 on operator instruction, backup
      at /tmp/crontab-backup-20260610.txt; env -i with the new PATH resolves
      python3 -> 3.12.11. Mode-1 (view-ordering): #842 merged — VIEW
      dispatch_success_by_role was created before dispatch_metadata existed in
      the SAVEPOINT transaction on legacy DBs, blocking v22/v23. VERIFIED
      2026-06-11 post-merge: vnx-dev quality_intelligence.db at user_version=23
      (v22/v23 self-healed), dispatch_success_by_role view present, quality_db
      _init reports operational. Root-cause history: cron without PATH ->
      system Python 3.9.6 -> 'Path | None' TypeError on module load, phase 0
      failed 9 nights (Jun 1-9), user_version stuck at 21, no data corruption.

  - feature_id: outcome-normalization
    title: "Dispatch-outcome integrity: weekly_digest classifier vocab + event-type filter, status-default policy, dispatch_metadata backfill (73% unknown root-caused)"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: in-progress
    pr_queue:
      - pr_id: "#837"
        title: "fix(digest): event-type filter + canonical outcome vocabulary (fix A + event_type fallback)"
        status: merged
        risk_class: medium
      - pr_id: "#847"
        title: "feat(outcome): dispatch_metadata backfill tool + contract_invalid vocab sync (gate-F2)"
        status: merged
        risk_class: medium
    notes: >
      Root-caused 2026-06-10, three layered gaps: RC-1 (62%)
      report_parser.py:251,257 defaults status='unknown' for tmux reports
      without a status line; RC-2 (14%) state_mutation infra events counted as
      dispatch outcomes; RC-3 (23%) 'contract_invalid' missing from failure
      vocabulary. Fix A (S, ready): weekly_digest.py:127-133 event-type skip
      set + canonical status sets (mirror check_active_drain.py:79-80);
      recovers ~37% immediately and moves contract_invalid to failure. Fix B
      (OPERATOR POLICY DECISION): default tmux-report status to 'done' instead
      of 'unknown' = 'no explicit failure -> inferred success'; do not
      implement without explicit sign-off. Backfill (M): 568 receipts with
      real dispatch_id insertable into dispatch_metadata (which holds only 23
      rows -> FPY/rework currently rest on a 23-row sample); 830
      dispatch_id='unknown' records are unfixable without report re-parse.
      STATUS 2026-06-11: fix A MERGED (#837, kimi-gated; gate finding F1 —
      lost event_type fallback for status-less receipts — was real and fixed
      forward with 4 extra tests, 41/41 green). gate-F2 CLOSED via #847:
      contract_invalid added to receipt_classifier._FAILURE_STATUSES +
      payload.FAILURE_STATUSES (cross-module vocab-sync test guards drift) and
      the dispatch_metadata backfill TOOL shipped
      (scripts/backfill_dispatch_metadata.py, dry-run default, --apply
      --backup, 67 tests). Fix-forward F1-F4 on #847 documented in the PR
      comment (payload SUCCESS_STATUSES missing 'done', task_timeout comment,
      _RANK ordering, silent skip-path logging). STILL OPEN (keeps this
      in-progress): (a) operator must RUN the backfill on the live DB
      (dispatch_metadata holds 23 rows; ~568 insertable) — tool is staged, run
      is an operator gate; (b) fix B status-default policy decision (default
      tmux-report status to 'done' vs 'unknown' — needs explicit sign-off).

  - feature_id: adr007-composite-keys-batch
    title: "ADR-007 batch: project_id + composite UNIQUE on SPC/intelligence tables (governance_metrics, spc_*, success_patterns, antipatterns, prevention_rules, dispatch_experiments, retry_budgets, dream_pattern_archives)"
    risk_class: high
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on:
      - nightly-pipeline-python-compat
    milestone: "1.0.1"
    status: planned
    notes: >
      Sweep R6 (verdict not-ready) + panel arbitration 2:1 deferred to 1.0.1
      with release note (Kimi-security dissented: ship-blocker). Single-project
      is the supported 1.0 mode; this is first-batch 1.0.1 work done as a real
      database-engineer-led migration, not under launch pressure. Includes:
      governance_metrics/spc_control_limits/spc_alerts have NO project_id at
      all (quality_intelligence.sql:614-658); success_patterns cluster has
      project_id without composite UNIQUE; dispatch_experiments
      UNIQUE(dispatch_id) single-tenant (quality_db_init.py:606);
      retry_budgets absent from 0010 migration list; dream_pattern_archives
      no composite. Depends on nightly fix so v22/v23 have applied first.

  - feature_id: dashboard-ops-completion
    title: "Dashboard operational completion: launch-script PORT export, /api/events/stream lane-aware migration, README port truth"
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#833"
        title: "feat(dashboard): lane-aware agent stream (incl. ops-completion commit 54b96194)"
        status: merged
        risk_class: low
    notes: >
      CLOSED 2026-06-10: landed as commit 54b96194 on PR #833 (merged).
      export PORT in launch-dashboard.sh; handle_events_stream migrated to
      the lane-aware _LANE_RE validation (regex = path-safety boundary); 5
      pages' error messages + README diagram corrected to real ports
      (backend 4174, Next.js dev 3100). Verified: py_compile + bash -n clean,
      test_api_intelligence 25 passed (2 pre-existing failures confirmed on
      clean tree via stash). Still open as watch-item (not blocking):
      list_lanes() O(2N) scan-per-poll at scale.

  - feature_id: oi-lifecycle-closure
    title: "Track/OI lifecycle closure path: vnx track done, oi-close/unlink_open_item, dispatch<->track linkage migration, vnx status --tracks, queue hygiene"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#849"
        title: "feat(tracks): oi-lifecycle-closure — track done, oi-close, dispatch linkage backfill, status --tracks"
        status: merged
        risk_class: medium
    notes: >
      CLOSED 2026-06-11 via #849 (130/130 tests, kimi-gated, fix-forward
      4f53ad1b). Closes the parent-child chain that was broken at every joint:
      adds 'vnx track done' CLI (mandatory --reason, binds the unbound
      transition_phase at tracks.py:338), 'vnx track oi-close' /
      unlink_open_item (blocked-status now closable without manual row delete),
      dispatch<->track linkage backfill migration (was zero — all 291
      dispatches on legacy A/B/C letters), 'vnx status --tracks', and central-DB
      track migrations so the live vnx-dev runtime DB carries track tables.
      This is the 'future state works like clockwork' feature. Ops-sweep RD
      origin: track -> dispatch -> receipt -> closure chain previously broken,
      13/36 tracks phase/derived drift, 235 stale files in dispatches/active/,
      open_items.json (154 open) not bridged to track_open_items.

  - feature_id: otel-wire-or-degrade
    title: "OTel decision: wire OTEL_EXPORTER_OTLP_ENDPOINT (+ tmux-lane callpoints) or degrade otel deps to [observability] extra"
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      Ops-sweep RB: otel_emitter.py:8 is a no-op without
      OTEL_EXPORTER_OTLP_ENDPOINT (set nowhere); only callsite is the
      subprocess lane (recovery.py:221,298), tmux lane has none; the three
      hard-pinned otel deps (pyproject.toml:15) currently buy zero
      observability for pip users (sweep R1 flagged install-footprint).
      Decide: local OTLP backend + tmux callpoints, OR move deps to an extra
      with lazy import. Half-state is the worst option. Also: smart_tap daemon
      not running (decide: required or retire); kimi receipts record
      token_usage 0/0 instead of explicitly-unavailable (silent-zero
      antipattern, T0-observed on live ledger 2026-06-09).

  - feature_id: sweep-hardening-batch1
    title: "Sweep hardening batch 1: state-dir guard, routing-policy conflict, tmux receipt truth, extra-flags quoting, broker atomicity, stderr drain, provider-CLI bypass guard"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#843"
        title: "fix(observability): unify state/events path resolution + events_path receipt pointer (H2, item 1)"
        status: merged
        risk_class: medium
      - pr_id: "#844"
        title: "fix(routing): resolve kimi lane/constraint conflict + generalize raw-spawn guard to provider CLIs (items 2+7)"
        status: merged
        risk_class: medium
      - pr_id: "#845"
        title: "fix(tmux-lane): truthful completion receipts + extra-flags quoting (H3/H5, items 3+4)"
        status: merged
        risk_class: medium
      - pr_id: "#848"
        title: "fix(dispatch): broker atomicity (orphan window, claim TOCTOU) + adapter pipe hygiene (H1/H6, items 5+6)"
        status: merged
        risk_class: medium
    notes: >
      CLOSED 2026-06-11: all 7 sweep-hardening items shipped across 4
      kimi-gated PRs. (1) state-dir guard -> #843 (VNX_DATA_DIR_EXPLICIT
      honoured; also adds events_path receipt pointer for observability
      linkage); (2) routing_policy kimi lane/constraint conflict -> #844
      (kimi-via-cli-only no longer collides with litellm:moonshot under
      VNX_AUTO_ROUTE=1); (3) tmux receipt truth + (4) --extra-flags shlex
      allowlist -> #845 (status no longer hardcoded done, timestamp evaluated
      at execution via $_VNX_TS, shell-injection rejected; T0-direct-reviewed
      PASS after 3x kimi-timeout, 129/129 PR tests green); (5) broker atomicity
      orphan-window/claim TOCTOU + (6) SubprocessAdapter stderr drain -> #848;
      (7) raw-spawn receipt-bypass guard generalized beyond claude -p to
      kimi/codex CLIs -> #844 (closes the T0-live-observed gap where a raw kimi
      call passed the hook unlogged on 2026-06-09).

  - feature_id: roadmap-governance-hygiene
    title: "Roadmap/docs governance hygiene: PM-mutator enforcement, drift-snapshot freshness, stale ROADMAP docs archive, docs-truth pass"
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: in-progress
    pr_queue:
      - pr_id: "#846"
        title: "docs: truth-pass for 1.0 launch — version labels, ADR amendments, surface sync, provenance"
        status: merged
        risk_class: low
    notes: >
      Ops-sweep RC. DOCS-TRUTH SUB-ITEM CLOSED 2026-06-11 via #846
      (docs-only, no gate required): version labels corrected, ADR-015 Path-D
      amendment landed, pip-surface list synced (migrate/track/dream),
      provenance pass. STILL OPEN (keeps this in-progress): PM-skill-sole-
      mutator is prose only — add a guard (commit touching ROADMAP.yaml must
      reference a dispatch-ID, or explicitly document it as convention);
      planning_drift.json was 7 days stale with 13 divergences — run
      track_reconciler in the nightly pipeline; archive
      claudedocs/strategy/ROADMAP.md (May, refers to nonexistent companion
      SSOT) and update docs/manifesto/ROADMAP.md (unaware of 1.0.1/1.1 cuts).

  - feature_id: injection-history-suppression
    title: "Self-learning quality: injection-history-aware suppression breaks 93% duplicate dominance"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on:
      - gap4-self-learning-reactivation
    milestone: "1.0.1"
    status: done
    pr_queue:
      - pr_id: "#850"
        title: "fix(selflearn): injection-history-aware suppression breaks duplicate dominance (93% dup root-cause)"
        status: merged
        risk_class: medium
    notes: >
      CLOSED 2026-06-11 via #850 (kimi-gated PASS). Ops-sweep finding: the
      reactivated self-learning loop (GAP 4) was re-injecting ~93% duplicate
      patterns — a handful of high-frequency patterns dominated every
      injection cycle, starving diversity and making the loop low-value
      despite running nightly. Root-caused to history-blind selection. Fix
      makes injection selection injection-history-aware so already-injected
      patterns are suppressed, breaking the duplicate-dominance feedback loop
      and restoring injection diversity. This is the substance behind the
      operator question 'does the self-learning loop work correctly again' —
      the loop ran but its output was near-duplicate noise; now it surfaces
      fresh patterns.

  # ---- 2026-06-14 PM-reconcile: future-state rebuild batch status + local-model PM-gate plan ----
  # Source: the 2026-06-13/14 future-state-rebuild (FSR) batch on main and the
  # pm-gate-agent-automation design (event-stream-primary measurement substrate).

  - feature_id: future-state-rebuild-batch
    title: "Future-state rebuild batch (FSR): ADR-007 in-place dispatches rebuild + version-reconciliation manifest + tenant-scoped build_t0_state + OI->track bridge -> autopilot_tick wiring"
    risk_class: high
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on:
      - oi-lifecycle-closure
      - planning-future-state-layer
    milestone: "1.0.1"
    status: in-progress
    pr_queue:
      - pr_id: "#857"
        title: "PR-0 (future-state): enforced test-isolation guard (no test mutates the live DB)"
        status: merged
        risk_class: high
      - pr_id: "#858"
        title: "PR-E (future-state): kanban honesty — degraded signal, no silent drops, staleness truth"
        status: merged
        risk_class: low
      - pr_id: "#859"
        title: "feat(fsr-a1): ADR-007 dispatches in-place rebuild — composite UNIQUE, 12-step, fail-closed tenant"
        status: merged
        risk_class: high
      - pr_id: "#861"
        title: "feat(fsr-a2): version reconciliation via declarative invariant manifest"
        status: merged
        risk_class: high
      - pr_id: "#863"
        title: "feat(fsr-b): tenant-scoped canonical tracks in build_t0_state"
        status: merged
        risk_class: high
      - pr_id: "#862"
        title: "feat(fsr-c): open-item→track bridge through tracks.py (R4.1–R4.4, D1/D3/D5)"
        status: merged
        risk_class: high
      - pr_id: "#871"
        title: "feat(fsr-d): OI->track bridge wiring into autopilot_tick"
        status: in_progress
        risk_class: high
    notes: >
      Future-state rebuild batch — the live activation step beyond
      planning-future-state-layer (build-complete, shipped-dark) and
      oi-lifecycle-closure (#849). HONEST STATUS as of 2026-06-14: PR-0
      isolation guard (#857) and PR-E kanban honesty (#858) merged earlier;
      PR-A1 ADR-007 dispatches in-place rebuild (#859), PR-A2 version-
      reconciliation invariant manifest (#861), PR-B tenant-scoped
      build_t0_state (#863), and PR-C OI->track bridge through tracks.py
      (#862) all merged to main today. PR-D — the bridge wiring into
      autopilot_tick (#871) — is IN PROGRESS (landing), NOT merged; tracked
      here as in_progress so it surfaces in the PR_QUEUE queued section and is
      not overstated as done. Remaining after PR-D: the operator
      runtime-migration runbook (§7.2, human-gated) — see #869. This batch is
      a real database-engineer-led ADR-007 migration of the dispatches/tracks
      future-state, not the SPC/intelligence-table batch (that is
      adr007-composite-keys-batch / #864, still planned and separate).

  - feature_id: pm-gate-agent-automation
    title: "PM-gate agent automation — event-driven per-receipt roadmap maintenance"
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on:
      - planning-future-state-layer
      - oi-lifecycle-closure
      - gemma-4-12b-integration
    milestone: "1.x"
    status: planned
    notes: >
      The PM-gate (roadmap mutation / track-close decision) need not be
      human-driven. A tiered model-agent runs the PM checks per receipt:
      deterministic / high-confidence closes are auto-applied (each with a
      receipt for the audit trail); ambiguous cases escalate to a stronger
      model or the human gate, preserving human-in-the-loop on the judgment
      tail. Key design points: (1) EVENT-DRIVEN per-receipt — fired by the
      receipt processor on each dispatch completion, NOT a living daemon, so
      it runs exactly when there is new state to reconcile. (2) TIERED MODEL —
      a cheap LOCAL model (gemma-4-12B via Ollama on the Mac Mini, or haiku)
      handles the high-frequency mechanical checks: zero API cost, fast, and
      Anthropic-cutoff-resilient (the local tier keeps working if the
      subscription/headless lane is disrupted); judgment cases escalate to
      sonnet or the human. (3) The advisory rollup reconciler already computes
      derived_status deterministically with no LLM (planning-future-state-
      layer Phase 3, #793), so the agent only handles the judgment tail on top
      of the deterministic truth — deterministic auto-close, ambiguous to the
      human gate. Relates to the 1.x items already filed as issues: #866
      (event-stream-primary measurement — the agent's normalized input
      substrate), #867 (OI-bridge exactly-once outbox), #868 (governance
      observability), #869 (operator runbook automation), #864 (broader
      ADR-007 batch). Depends on the reconciler (deterministic derived_status)
      and the local-lane evaluation (gemma-4-12b-integration) being in place.

  # ============================================================================
  # ---- Post-1.0 repo-audit remediation (multi-provider swarm, 2026-06-27) ----
  # Source: claudedocs/2026-06-27-repo-audit-findings.md (1 critical, 16 high,
  # 41 medium, 39 low after dedup; 12 dimensions x 6 providers, adversarial
  # verify). plan = notes; execution gates = review_stack. Operator directive:
  # fill roadmap with plan + gates, then execute autonomously (human gate only
  # for the PyPI ship). Ship-blockers = milestone 1.0; cleanup = 1.0.1.
  # ============================================================================

  - feature_id: audit-sec-dashboard-auth
    title: "Dashboard: default loopback bind + token/CSRF auth on mutating POSTs (audit critical #1)"
    branch_name: feat/audit-dashboard-auth
    risk_class: high
    merge_policy: human
    review_stack:
      - codex_gate
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      CRITICAL. serve_dashboard.py binds ("::",port) with IPV6_V6ONLY=0 (all
      interfaces) while the banner says localhost; no auth; ACAO:* on every
      mutation. POST /api/restart-process pkills+respawns the daemon fleet and
      /api/operator/* (incl. config/set, gate/toggle) flips governance. Fix:
      default-bind 127.0.0.1 (VNX_DASHBOARD_BIND opt-in for remote), fix the
      banner, drop blanket ACAO:*, add a shared-secret token + Origin/CSRF
      guard on every mutating POST, refuse mutation on a non-loopback bind
      without a token. File: dashboard/serve_dashboard.py:685,205,654-668.

  - feature_id: audit-dep-pyyaml
    title: "Add missing pyyaml runtime dependency (audit high #5)"
    branch_name: feat/audit-dep-pyyaml
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      constraint_enforcer.py:44 does an unguarded import yaml; pyproject omits
      pyyaml, so the headline hello-world dispatch dies with No module named
      yaml on a clean install. Fix: add "pyyaml>=6.0" to project.dependencies
      (pyproject.toml:13).

  - feature_id: audit-docs-pip-install
    title: "Fix pip-install-not-on-PyPI doc contradiction (audit high #4)"
    branch_name: feat/audit-docs-pip-install
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      QUICKSTART.md:9-12 + ONBOARDING Step 1 lead with `pip install
      vnx-orchestration` (not on PyPI; README:66 contradicts). Fix: make Step 1
      match README (clone + pip install -e .) with a "not on PyPI yet" caveat,
      until the publish.

  - feature_id: audit-demo-modernize
    title: "Demo: retire the redundant 4-tmux T1-T3 model (audit high #9)"
    branch_name: feat/audit-demo-modernize
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      README markets `demo` as the no-key entry point but the assets are
      missing/untracked and `./bin/vnx demo` errors. Operator note: the demo is
      the old 4-tmux-window T1-T3 model, now redundant (door + leaseless lane).
      Fix: remove the demo command + its 3 README refs, OR ship a minimal
      current door-model dry-run replay. Do NOT restore the stale 4-window
      assets. File: scripts/lib/vnx_demo.py:172-176,42-53.

  - feature_id: audit-dx-doctor-worker-cli
    title: "vnx doctor: probe worker CLIs it dispatches to (audit high #7)"
    branch_name: feat/audit-doctor-worker-cli
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      doctor.py:32-47 probes only python3/git/jq/shellcheck — no claude/codex/
      gemini/kimi. Fix: add a WARN-level worker-CLI probe (claude, optionally
      others) with an actionable install message. Highest-leverage first-run fix.

  - feature_id: audit-dx-dispatch-preflight
    title: "dispatch-agent: preflight claude binary + surface the failure reason (audit high #6)"
    branch_name: feat/audit-dispatch-preflight
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      dispatch_agent.py:93 Popen(["claude",...]) with no shutil.which precheck;
      a missing/unauthenticated CLI prints only status: failed. Fix: add a
      which('claude') precheck + on a False door result read the failure_class
      and print the exit_classifier reason + a `vnx doctor` hint.

  - feature_id: audit-dx-prerequisites-docs
    title: "Add a Prerequisites section to the pip happy-path (audit high #8)"
    branch_name: feat/audit-prereq-docs
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      No prerequisites section anywhere; the onboarding starter path presents
      dispatch-agent as the first task with no mention of the worker CLI it
      needs. Fix: add a Prerequisites block to README install + onboarding
      (VNX drives existing coding CLIs; default lane needs an installed+
      authenticated claude CLI + incurs usage; point zero-key users at the demo
      replacement).

  - feature_id: audit-gov-report-parser-contract
    title: "report_parser: enforce body-contract + phantom-guard before task_complete (audit high #10)"
    branch_name: feat/audit-report-parser-contract
    risk_class: high
    merge_policy: human
    review_stack:
      - codex_gate
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      report_parser.py:165-172,652-668 (the canonical receipt-processor daemon)
      converts any unified_reports/*.md to a clean task_complete with NO body
      contract and NO phantom-guard — a GATE-GREEN/success report with no code
      change is admitted as status=success. The primary audit-trail producer
      accepting unverified completions. Fix: run report_body_contract.
      validate_body() + on failure/filename-only dispatch_id emit
      report_contract_invalid/status=contract_invalid; treat status='unknown'
      on a completion event as a violation.

  - feature_id: audit-gov-sidedoor-ci
    title: "Enforce the dispatch side-door audit in CI (audit high #2)"
    branch_name: feat/audit-sidedoor-ci
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      dispatch_sidedoor_audit.py main() returns 1 on a new side-door but nothing
      in CI runs it; the pytest allowlist omits test_dispatch_sidedoor_audit.py
      + test_runtime_adapter_certification.py; no pre-commit. Fix: add a CI step
      running the audit (fail on exit 1) + add the two test files to a pytest
      step. (.github/workflows/vnx-ci.yml:72-214)

  - feature_id: audit-correctness-receipt-idempotency
    title: "Receipt idempotency: write-ahead the dedup marker (audit high #3)"
    branch_name: feat/audit-receipt-idempotency
    risk_class: high
    merge_policy: human
    review_stack:
      - codex_gate
      - kimi_review
    depends_on: []
    milestone: "1.0"
    status: planned
    notes: >
      append_receipt_internals/idempotency.py:196-203 appends the receipt before
      recording the idempotency key via _write_cache; a cache-write failure
      after the receipt write means a retry re-appends the same audit receipt.
      Fix: record the dedup marker BEFORE writing the receipt (worst case a
      missing receipt, never a duplicate), or downgrade a post-write _write_cache
      failure to a best-effort log.

  - feature_id: audit-tests-build-project-status
    title: "Fix brittle source-grep test (audit high #11)"
    branch_name: feat/audit-test-build-project-status
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      test_build_project_status.py:263-268 asserts 'write_project_status(_STATE_DIR)'
      but prod renamed to state_dir. Bare FAIL, no regression. Fix: behavioral
      test (mock write_project_status, assert called with the right Path) or
      match the new call string.

  - feature_id: audit-tests-build-t0-brief
    title: "Fix build_t0_brief rc=1-on-empty-state_dir tests (audit high #12)"
    branch_name: feat/audit-test-build-t0-brief
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      test_build_t0_brief_output.py:94,116,126,146 assert rc==0 on an empty
      state_dir but _build_system_health returns degraded so main() exits 1.
      Fix: test the pure fns (build_t0_state + _state_to_brief/_resolve_output_path)
      or seed minimal state + mock the health-beacon.

  - feature_id: audit-tests-fsr-b-tenant-guard
    title: "Fix tenant-guard test escaping tmp isolation (audit high #13)"
    branch_name: feat/audit-test-fsr-b-tenant-guard
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      test_fsr_b_tenant_guard.py:225-244 — _resolve_tracks_store calls
      resolve_central_data_dir('vnx-dev') and reads the real production store,
      bypassing the monkeypatched VNX_DATA_DIR. Fix: monkeypatch
      resolve_central_data_dir->None (and/or VNX_USE_CENTRAL_DB='') before
      build_t0_state() in the canary; add a builder-layer resolver test.

  - feature_id: audit-tests-fsr-isolation-guard
    title: "Fix isolation-guard fixture missing project-id marker (audit high #14)"
    branch_name: feat/audit-test-fsr-isolation-guard
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      test_fsr_isolation_guard.py:149,192,208,255 — _make_v21_project never
      writes .vnx-project-id, so the fail-closed _resolve_validated_project_id
      (D-A1 #913, ADR-007 R3.1) raises before assertions. Fix: write
      (project_dir/'.vnx-project-id').write_text('vnx-dev\n') in the fixture.

  - feature_id: audit-tests-feedback-loop
    title: "Fix feedback-loop silent column-swallow (audit high #15)"
    branch_name: feat/audit-test-feedback-loop
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      api_intelligence.py:847-848 swallows OperationalError (no such column:
      project_id) -> [] -> 200 with all-zero metrics; test_feedback_loop.py:350
      asserts boosts==1. Fix: discriminating except (re-raise unless 'no such
      table'); add project_id to the test schema + INSERTs.

  - feature_id: audit-docs-autopilot-flag
    title: "Fix non-existent VNX_AUTOPILOT flag in docs (audit high #16)"
    branch_name: feat/audit-docs-autopilot-flag
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      ROADMAP.md:98, MULTI_TRACK_PARALLEL_EXECUTION_CONTRACT.md:125, ADR-020:21
      gate RA-6 on VNX_AUTOPILOT=1 (zero hits in scripts/); the real flag is
      VNX_ROADMAP_AUTOPILOT. Fix: replace the flag name in all three docs.

  - feature_id: audit-sweep-medium
    title: "Repo-audit medium-severity sweep (41 findings)"
    branch_name: feat/audit-sweep-medium
    risk_class: medium
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      The 41 medium findings from claudedocs/2026-06-27-repo-audit-findings.md
      across security/correctness/architecture/hardening/tests/docs/ai-slop/
      perf/governance/oss-readiness. Worked in batched gated PRs by cluster.

  - feature_id: audit-sweep-low
    title: "Repo-audit low-severity / AI-slop sweep (39 findings)"
    branch_name: feat/audit-sweep-low
    risk_class: low
    merge_policy: human
    review_stack:
      - kimi_review
    depends_on: []
    milestone: "1.0.1"
    status: planned
    notes: >
      The 39 low findings (dead code, defensive bloat, comment-noise, nits) from
      the audit doc. Worked as a final cleanup sweep in batched gated PRs.
