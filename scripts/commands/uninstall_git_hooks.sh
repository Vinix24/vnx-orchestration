#!/usr/bin/env bash
# Wrapper for the git hook uninstall command.
# Loads cmd_uninstall_git_hooks from install_git_hooks.sh.

# shellcheck source=/dev/null
source "$VNX_COMMANDS_DIR/install_git_hooks.sh"
