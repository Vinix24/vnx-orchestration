# Full-suite CI test exclusions (OI-906).
#
# profile-a in .github/workflows/vnx-ci.yml runs pytest "$GITHUB_WORKSPACE/tests"
# at directory level (the real checkout, not the vendored .claude/vnx-system
# copy — measured 2026-08-02: the vendored tree produces ~197 layout-artifact
# failures, not code reds). This file is the EXPLICIT exclusion list: every
# path below is red on main (measured 2026-08-01: pytest tests/ --ignore=tests/f39,
# then a sequential confirmation run of the candidates) and is excluded from
# the CI sweep until its repair lands. Nothing is excluded silently.
#
# FORMAT (enforced by the test-exclusion-reason gate in scripts/local-ci.sh via
# scripts/ci/check_test_exclusions.py): every entry MUST carry an inline reason
# ("# <why>"). An entry without a reason, a path that no longer exists, or a
# duplicate path fails local CI. Reason style: the tracking OI plus what was
# measured (red solo with <error>, green solo but red in the full-suite sweep,
# or hangs). A bare path reads as "silently disabled" — the drift this gate
# closes.
#
# Entries with a "CI image" reason are green on main locally but cannot run in
# the minimal CI image (undeclared heavy dependency stack); each names the
# missing stack and the repair path to rejoin the sweep.
#
# Tracking item: OI-906 ("CI draait 18 van de 904 testbestanden"). Each
# entry needs its own repair dispatch before it can rejoin the suite.
#
# test_dispatch_envelope.py is NOT listed here: only TestFlagGateClaude is red
# (4 tests), and the workflow filters it out with -k "not TestFlagGateClaude" so
# the file's 29 green tests still run.
#
# test_receipt_mirror_isolation.py is NOT listed here either: only
# TestGovernEnsureReceiptIsolation::test_ensure_receipt_does_not_touch_real_store
# is red, and ONLY in full-suite collection order on CI — it passes in
# isolation, including after the merge of main. Measured on head 9a01f5cc:
# 1 failed, 15953 passed, where this test is the 1. The stub-teardown in
# tests/test_intelligence_daemon_paths.py fixed its four order-dependent
# sibling tests plus 13 collateral tests (test_learning_loop_tz,
# test_w5b_small_fixes); this one is a different, still unattributed cause and
# a collection-order bisect was deliberately NOT attempted. The workflow
# filters it out by name in the same -k expression so the file's other seven
# tests keep running (OI-1051 versmald, PR #1434).
# OI-1213 recheck (2026-08-15): not the preflight-hook mechanism — solo 8 green,
# plus tests/test_mc_v4_columns.py neighbour 32 green. Root cause stays the
# unattributed collection-order effect (OI-1121).
#
# dispatch 20260802-w20b-1310-red-classify (2026-08-02) added the 42 files below
# citing OI-946..OI-953: PR #1310 widened this same sweep from 831 files (the
# ones NOT already listed above) to include these too, and 213 of their tests
# came back red in CI. Classified A/B by running the exact same full-suite
# composition (this file's exclusions + --ignore=tests/f39) on a fresh
# origin/main worktree: 218 failed there too, with 15699 passed on BOTH sides —
# 204 of the 213 branch reds reproduce byte-for-byte on main, and the
# remaining 9 pass in isolation on both main and the branch (order/load-
# dependent, OI-658 class). Zero of the 213 are caused by PR #1310 itself
# (which only touches this workflow, this file, and two conftest.py files —
# no application code). Each new entry cites the OI covering its cluster.
#
# 2026-08-11 de-quarantine (OI-1131 + OI-1133): 29 files removed from this
# list after each ran fully green SOLO on main 12f0b4ee in a plain clone
# (120s per-file timeout) — the 23 files OI-1133 measured green, the OI-949
# dispatch-register/event-log cluster (5 files, repaired by #1447), and
# test_subprocess_dispatch_worktree_isolation.py (7 reds of OI-1131,
# repaired by #1449; reproduced at pre-fix SHA 30d6916b as control).
# Solo-green is not sweep-green: the de-quarantining PR's own Profile A run
# is the full-sweep group check per OI-1133. Files still red stay listed.
# GROUP-CHECK OUTCOME (PR #1458 Profile A, run 31506897662): 6 of the 29
# flipped red in the full sweep on the CI image (35 fails, all inside those
# 6 files) and returned to this list with the failure named per entry.
# 23 files (491 tests) stay de-quarantined; that sweep had no other reds.
#
# 2026-08-15 OI-1213 re-assessment: the six OI-1133 group-check entries below
# were re-checked against the OI-1213 preflight-hook mechanism
# (migrate_future_system registers register_preflight(30, ...) at import; a
# fixture that skips 0029 and applies 0030 fails once that import happens).
# Verdict: NONE of the six is OI-1213 (clean-env solo + tests/test_mc_v4_columns.py
# neighbour all green; none has a 0029/0030 skip fixture). Two of them
# (test_receipt_processor_autopr_rejected_mirror.py, test_receipt_processor_bootstrap.py)
# are red in the sweep for a now-explained reason: the vnx_paths.sh cross-project
# guard (scripts/lib/vnx_paths.sh:221-228) unsets an explicit VNX_STATE_DIR pin
# when VNX_HOME points elsewhere (on CI: the vendored $WORKSPACE/.claude/vnx-system),
# then falls back to $VNX_DATA_DIR/state. There is a SECOND trigger, order-dependent:
# vnx_init calls vnx_paths.ensure_env() (scripts/vnx_init.py:736 ->
# scripts/lib/vnx_paths.py:570-575) which does os.environ.setdefault("VNX_HOME", ...);
# in a combined run test_init_migrate_bootstrap.py injects VNX_HOME into the process
# env, and the two receipt-processor tests spawn bash WITHOUT env= so they inherit it.
# The fix lives in vnx_paths.sh + the subprocess env pinning, not in these files.
# Full write-up: claudedocs/test-quarantaine-verklaring-20260815.md.
#
tests/integration/test_gemini_streaming_gated.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/migrations/test_auto_apply.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/pool/test_pool_lease_e2e.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_adapter_conformance.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_adr007_dispatches_repair.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_adr007_structural_conformance.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_agent_stream_sse.py  # OI-906: hangs a single-process run (loops in time.sleep forever), red on main (see header)
tests/test_agent_validation.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_auto_commit_stash_isolation.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_auto_report_integration.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_behavior_contracts.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_bench_headless_routing.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_bench_seed_integrity.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_billing_safety.py  # OI-951: doc/content drift the test already asserts against, red on main too
tests/test_build_t0_state_exception_handling.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_burnin_certification.py  # hangs without VNX_HEADLESS_CLI=echo; covered by burn-in-headless.yml
tests/test_busy_terminal_certification.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_ci_gate.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_claude_github_review_linkage.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_cleanup_stale_vnx_sessions.py  # OI-952: order/load-dependent flake, passes solo on both main and branch (OI-658 class)
tests/test_cli_flag_forwarding.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_cli_init.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_close_track_if_done.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_codex_pr311_findings.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_codex_r2_fixes.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_constraint_enforcer.py  # OI-952: order/load-dependent flake, passes solo on both main and branch (OI-658 class)
tests/test_context_rotation.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_conversation_analyzer.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_conversation_resume.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dashboard_feature.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dashboard_kanban_certification.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dashboard_sse_empty_file.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_delivery_failure_codes.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dispatch_agent_default_instruction.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dispatch_daemon.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dispatch_manifest.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dispatch_serialization.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_dispatcher_drain_lifecycle.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_error_contracts.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_future_state_reconciliation.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_gate_dispatch_id.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_gate_evidence_accuracy.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_gate_execution_certification.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_gate_runner.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_gate_status_schema.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_gemini_prompt_no_duplicate.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_governance_blockers_fix.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_governance_digest_certification.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_governance_digest_runner.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_headless_dispatch_daemon_wave5.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_headless_inspect.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
# tests/test_horizon_parity.py — REPAIRED (OI-1065): the pip-CLI/engine flag drift
# (vnx horizon add --lane-hint, close --attest/--pr/--max-gh-calls, plan-gate
# attest delegation) was the red this exclusion hid. Suite is 85/85 green and
# now rejoins the Profile A sweep so new drift fails the PR, not a red suite
# nobody reads. No heavy-dep stack (sqlite3 + in-repo modules only).
tests/test_horizon_schema_migration_fix.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_horizon_skill_alias.py  # OI-951: doc/content drift the test already asserts against, red on main too
tests/test_init_migrate_bootstrap.py  # OI-1133 group-check: 18/32 failed in PR#1458 Profile A sweep (vnx init/migrate exit non-zero on the CI image; OI-946 class). Not OI-1213 (2026-08-15): autouse fixture already isolates _PREFLIGHT_HOOKS; 56 green clean-env solo+neighbour
tests/test_installer_no_template_leak.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_intelligence_ab_framework.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_intelligence_pipeline_e2e.py  # OI-947: learning_loop.ensure_env AttributeError, red on main too
tests/test_learning_feature.py  # OI-947: learning_loop.ensure_env AttributeError, red on main too
tests/test_learning_loop_exception_handling.py  # OI-947: learning_loop.ensure_env AttributeError, red on main too
tests/test_learning_loop_tz.py  # OI-947: learning_loop.ensure_env AttributeError, red on main too
tests/test_litellm_agentic_runner.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_llm_decision_router.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_migrate_future_system.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_migrate_pool_seed.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_observability_linkage.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_open_items_gate_certification.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_pattern_id_stability.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_pattern_matching.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_per_pr_closure.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_plan_gate_panel.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_planning_api.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_planning_cli.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_planning_cli_objective.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_pool_manager_real_spawn.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_pool_provider_mix_integration.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_pool_state_repo.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_pr10_provider_string_canon.py  # OI-867: stale vs the deepseek proxy/harness split — 2 tests expect the registry key "deepseek", the split maps "deepseek_harness" (measured 2026-08-15: 2 failed, 38 passed)
tests/test_pr2_requeue_clear_context.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_project_status_hook.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_provider_dispatch_contract_integration.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_provider_dispatch_deepseek_harness.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_provider_dispatch_entry.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_quality_advisory_pipeline.py  # OI-1133 group-check: 2/43 failed in PR#1458 Profile A sweep (NoneType .status; OI-948 class). Not OI-1213 (2026-08-15): no migration fixture; 67 green clean-env solo+neighbour
tests/test_queue_reconciliation_certification.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_quickstart_validation.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_receipt_classifier.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_receipt_instruction_hash.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_receipt_processor_autopr_rejected_mirror.py  # OI-1133 group-check: 6/6 failed in PR#1458 Profile A sweep (outcome_status resolves None). Not OI-1213 (2026-08-15): VNX_HOME guard clobbers the explicit VNX_STATE_DIR pin (vnx_paths.sh:221-228) -> shell-lane DB update lands in $VNX_DATA_DIR/state; see header
tests/test_receipt_processor_bootstrap.py  # OI-1133 group-check: 6/10 failed in PR#1458 Profile A sweep (bootstrap returns rc=1). Not OI-1213 (2026-08-15): VNX_HOME guard clobbers VNX_STATE_DIR -> PROCESSING_LOG falls back to $VNX_DATA_DIR/state -> tee fails rc=1; see header
tests/test_receipt_t0_view_filter.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_receipt_v2_pr4_wiring.py  # OI-948: receipt v2 PR4/PR5: NoneType has no attribute status, red on main too
tests/test_receipt_v2_pr5_schema_cutover.py  # OI-948: receipt v2 PR4/PR5: NoneType has no attribute status, red on main too
tests/test_repo_map.py  # CI image: red in the minimal image on two measured stacks — without tree-sitter repo_map.py raises NameError (_TSParser, latent scripts/lib bug, repair needs own dispatch; scripts/lib is out of scope here), and the networkx PageRank tests need the undeclared networkx+numpy+scipy stack. Installing networkx alone makes it WORSE (27 failed vs 13): repo_map leaves its positional fallback and nx.pagerank crashes on missing numpy, which also breaks the green test_repo_map_provider_coverage.py.
tests/test_review_adapters.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_roadmap_consistency.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_routing_policy.py  # OI-952: order/load-dependent flake, passes solo on both main and branch (OI-658 class)
tests/test_runtime_coordination_schema_drift.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_runtime_facade_registry_integration.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_safe_autonomy_cutover.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_schema_init_view_ordering.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_semantics_cleanup.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_serve_dashboard_api.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_session_ids_staleness.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_sessionstart_hook.py  # OI-951: doc/content drift the test already asserts against, red on main too
tests/test_strategy_roadmap.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_strategy_roadmap_real_file.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_subprocess_adapter_pr3.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_subprocess_cheap_lane.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_subprocess_dispatch_identity.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_subprocess_health.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_subprocess_w3e_robustness.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_t0_decision_log.py  # OI-1133 group-check: 2/60 failed in PR#1458 Profile A sweep (same-length source-replacement cursor detection). Not OI-1213 (2026-08-15): no migration fixture; green clean-env solo+neighbour
tests/test_t0_escalations_log.py  # OI-1133 group-check: 1/60 failed in PR#1458 Profile A sweep (same-length source-replacement cursor detection). Not OI-1213 (2026-08-15): no migration fixture; green clean-env solo+neighbour
tests/test_t0_skill_consistency.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_t0_skill_slim.py  # OI-951: doc/content drift the test already asserts against, red on main too
tests/test_tenant_stamp_fail_closed.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_terminal_state_reconciler.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_tmux_adapter.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_tmux_adapter_interface.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_tmux_interactive_dispatch.py  # fails / exceeds per-test timeout (OI-906)
tests/test_tmux_worktree.py  # OI-952: order/load-dependent flake, passes solo on both main and branch (OI-658 class)
tests/test_track_cli.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_track_v23_cli.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_vnx_cli.py  # OI-946: DB/migration precondition chain (init/migrate/track bootstrap) red on main too
tests/test_vnx_start_runtime.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_w5b_small_fixes.py  # OI-947: learning_loop.ensure_env AttributeError, red on main too
tests/test_w_init_project_id_threading.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_wave4_central_install_e2e.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/test_wave4_cmd_update.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/test_wave4_path_resolver.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/test_wave4_regen_settings_target.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/test_wave4_save_restore.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/test_wave4_write_guard.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/test_wave7_deepseek_smoke.py  # OI-867: stale vs the deepseek proxy/harness split — smoke routes litellm:deepseek to a model on dispatch_allowed: false (measured 2026-08-15: 1 failed, 12 passed)
tests/test_week_1_skills.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_wheel_install.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_wiring_completeness.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_worker_heartbeat.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
tests/test_worker_state_dir.py  # OI-950: wave4 central-install assumes GH Actions runner HOME layout, red on main too
tests/unit/test_codex_event_normalizer.py  # OI-906: red on main in the 2026-08-01 full-suite inventory; rejoin needs a repair dispatch (see header)
